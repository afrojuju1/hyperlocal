# SDXL Local Models

This directory is mounted into the SDXL WebUI container at `/sd/stable-diffusion-webui`.

Place your model files in:
`sdxl/models/Stable-diffusion/`

Example file:
`sdxl/models/Stable-diffusion/sdxl_turbo.safetensors`
